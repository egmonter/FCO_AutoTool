{
  "_comment": "Edit timeout values in seconds. Positive integers only.",
  "BOOT_TIMEOUT": 600,
  "BIOS_REBOOT_WAIT": 10,
  "BIOS_WAIT_TIMEOUT": 900,
  "BIOS_NUDGE_INTERVAL": 5,
  "SVOS_TIMEOUT": 600,
  "CENTOS_BOOT_TIMEOUT": 600,
  "MOUNTSV_TIMEOUT": 1800,
  "CMD_TIMEOUT": 120,
  "SC_TIMEOUT": 600,
  "ROCKET_TIMEOUT": 1200,
  "MEMIC_TIMEOUT": 2400,
  "MLC_TIMEOUT": 2400,
  "SOLAR_TIMEOUT": 1200,
  "OSVOSUPDATE_TIMEOUT": 900,
  "SVOSINFO_TIMEOUT": 60,
  "UPDATE_MOUNTSV_TIMEOUT": 900
}
